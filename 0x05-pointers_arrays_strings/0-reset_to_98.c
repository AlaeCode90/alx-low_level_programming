#include <stdio.h>

/** 
 * reset_to_98.c updates the value of the variable that the pointer points to 
 * the pointer points o 98
 * @n: is the pointer
 * return - always 0 if (successful)
 */

void reset_to_98(int *n);
{
	*n = 98;
}
