#include <stdio.h>
/**
 * main - a function that prints out a string to stdout.
 * Return: 0 if success.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
