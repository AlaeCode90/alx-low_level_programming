# include <stdio.h>
#include <stdlib.h>

/**
* main - Entry point
* 
* return - always 0 if (success)
*/

int main(void)
{
char c
if 
{
for ( c = 'A' ; c <= 'Z' ; c++)
putchar(c);
return (1);
}
else 
return(0);
}
